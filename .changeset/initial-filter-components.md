---
"music-table": minor
---

Initial filter component set: `SearchInput`, `SingleSelectFilter`, and
`MultiSelectFilter`, built on the shared `FilterPill` and `FilterPopover` primitives and a
common `FilterOption` shape. See `docs/rfcs/0001-filter-component-api.md` for the API rationale.
